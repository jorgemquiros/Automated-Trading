name: Automated Trading

on:
  schedule:
    - cron: '0 14 * * 1-5'  # 9am EST
  workflow_dispatch:

jobs:
  analysis:
    runs-on: ubuntu-latest
    steps:
      - name: Run Market Analysis
        run: curl -X POST "${{ secrets.ANALYSIS_URL }}"
  
  execution:
  needs: analysis
  runs-on: ubuntu-latest
    steps:
      - name: Execute Trades
        run: curl -X POST "${{ secrets.EXECUTION_URL }}"
